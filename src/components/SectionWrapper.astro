---
interface Props {
  id?: string;
  bg?: 'dark' | 'base' | 'light';
  class?: string;
  overflow?: 'hidden' | 'visible';
}
const { id, bg = 'dark', class: className, overflow = 'hidden' } = Astro.props;
const bgMap = {
  dark: 'var(--bg-dark)',
  base: 'var(--bg)',
  light: 'var(--bg-light)'
};
---
<section id={id} class:list={['section-wrap', className]} style={`background: ${bgMap[bg]}; overflow: ${overflow};`}>
  <div class="section-inner">
    <slot />
  </div>
</section>

<style>
  .section-wrap {
    padding: var(--space-12) var(--space-8);
  }
  .section-inner {
    max-width: var(--container-xl);
    margin: 0 auto;
  }
  @media (max-width: 768px) {
    .section-wrap {
      padding: var(--space-8) var(--space-6);
    }
  }
</style>
